import { createTheme } from "@mui/material";

export const themes = createTheme({
  palette: {
    primary: {
      main: "#3c1361",
      white: "#ffffff",
      bgColor: "#ffffff7e",
      dark: "#1c1f26",
      offwhite: "#d4c5c5",
      grey: "#ededed",
    },
  },
});
